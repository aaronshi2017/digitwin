# Digitwin

Digital Twin AI project for network congestion control using ns-3 simulation and Docker-based iperf3 clients.

## Installation

```bash
git clone https://github.com/<username>/digitwin.git
cd digitwin
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
